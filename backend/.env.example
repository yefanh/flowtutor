# Copy to backend/.env and adjust if needed.
# Port 5433 matches the mapping in docker-compose.yml.
DATABASE_URL=postgresql://flowtutor:flowtutor@localhost:5433/flowtutor

# Not used until Phase 2 (AI tutor). Left here as a placeholder.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
