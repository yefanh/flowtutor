[alembic]
script_location = alembic
prepend_sys_path = .
# Migration filenames get a numeric prefix so they sort in apply order.
file_template = %%(rev)s_%%(slug)s

# sqlalchemy.url is set at runtime in alembic/env.py from DATABASE_URL, so the
# database password never lives in a checked-in file.

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
